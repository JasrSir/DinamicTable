<?xml version="1.0" encoding="utf-8"?>
<!-- Table Header Style -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="3dp"
        android:color="@color/border_header"/>
    <!-- solid android:color="@color/background_header"/-->
    <gradient
        android:type="linear"
        android:startColor="@color/startBackground"
        android:endColor="@color/endBackground"/>

</shape>